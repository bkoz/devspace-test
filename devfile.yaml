schemaVersion: 2.1.0
metadata:
  name: devspace-test
components:
  - name: runtime
    container:
      image: quay.io/bkozdemb/base-developer-image:ubi9-latest
      volumeMounts:
        - name: venv
          path: /home/user/.venv
      memoryLimit: 2G
      mountSources: true
  - name: venv
    volume:
      size: 1G
commands:
  - id: run
    exec:
      label: "Run the application"
      component: python
      workingDir: ${PROJECTS_ROOT}/devspace-test
      commandLine: python -m venv .venv && . .venv/bin/activate && python --version
      group:
        kind: run
